name: Issue 101 frozen robustness run (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      OPENAI_API_KEY: ""
    steps:
      - name: Historical paid runner retired
        run: |
          echo "The issue #101 paid workflow is historical and no longer dispatches provider calls."
          echo "Its runner remains available for keyless reconstruction. New paid experiments must use Provider readiness canary plus Provider experiment."
          exit 1
