name: Live evaluation (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      OPENAI_API_KEY: ""
    steps:
      - name: Paid live evaluation moved behind provider reliability gates
        run: |
          echo "The legacy Live evaluation workflow is retired."
          echo "Use Provider readiness canary, freeze its successful evidence into a thorn-provider-experiment/2 manifest, then use Provider experiment."
          exit 1
