name: Issue 134 pre-125 robustness continuation (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      OPENAI_API_KEY: ""
    steps:
      - name: Historical paid runner retired
        run: |
          echo "The issue #134 pre-125 paid workflow is historical and no longer dispatches provider calls."
          echo "Its runner remains available for keyless reconstruction. New paid experiments must use Provider readiness canary plus Provider experiment."
          exit 1
